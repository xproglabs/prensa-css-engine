import {
  AlignTypes,
  AlignXTypes,
  AlignYTypes,
  ThemeTypes
} from '@xprog/prensa-css-engine/types'
export interface AlignProps {
  align?: AlignTypes;
  alignx?: AlignXTypes;
  aligny?: AlignYTypes;
  theme?: ThemeTypes;
}