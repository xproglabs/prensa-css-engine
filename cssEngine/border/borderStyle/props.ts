import { BorderStyleTypes, ThemeTypes } from '@xprog/prensa-css-engine/types'

export interface BorderStyleProps {
  borderStyle?: BorderStyleTypes;
  bs?: BorderStyleTypes;
  bst?: BorderStyleTypes;
  bsr?: BorderStyleTypes;
  bsb?: BorderStyleTypes;
  bsl?: BorderStyleTypes;
  bsx?: BorderStyleTypes;
  bsy?: BorderStyleTypes;
  theme?: ThemeTypes;
}